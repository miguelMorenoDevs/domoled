info:
  name: Spotify login
  type: http

http:
  method: POST
  url: http://localhost:3000/spotify/login
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
